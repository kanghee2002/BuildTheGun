namespace BuildTheGun.Combat.Effects
{
    public enum TriggerType
    {
        None,
        OnLoad,
        OnFire,
        OnHit,
        OnKill,
        OnReload,
        OnMerge,
        OnMagazineEmpty,
    }
}
